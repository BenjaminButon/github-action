import {{camelCase name}}Reducer, { {{pascalCase name}}State } from '@store/{{camelCase name}}'

describe('{{camelCase name}} reducer', () => {
  const initialState: {{pascalCase name}}State = {}

  it('should handle initial state', () => {})
})